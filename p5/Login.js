class Login
{

	constructor()
	{
		let inputText
		let header
	}
	userinterface()
	{
		header = createElement('h3', 'Login');
		header.position(width/4, height/2);

		inputText = createInput();
		inputText.position(width/4,height/2);
	}
}